services:
  laravel:
    volumes:
      - ./laravel-api:/var/www         # ← écrase le volume nommé en dev
  nginx:
    volumes:
      - ./laravel-api:/var/www         # ← idem pour Nginx en dev
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf